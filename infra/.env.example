# Copy this file to infra/.env and adjust values for your VM
# These will be loaded by docker-compose for both services

# Common
CONFIG_OUTPUT_ROOT=/app/output

# Scraper flags
CONFIG_USE_PROXIES=false
CONFIG_ONLY_REQUESTED_TICKERS=false
CONFIG_KEEP_DEBUG_IMAGES=false

# LLM (optional)
# GOOGLE_GENAI_API_KEY=
